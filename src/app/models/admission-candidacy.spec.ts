import { AdmissionCandidacy } from './admission-candidacy';

describe('AdmissionCandidacy', () => {
  it('should create an instance', () => {
    expect(new AdmissionCandidacy()).toBeTruthy();
  });
});
